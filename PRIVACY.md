# Privacy

MicroDeck is designed to operate locally.

- No OpenAI API key is required.
- No telemetry is enabled by default.
- Conversation content must not be written to logs.
- Credentials, tokens, and session data must never be collected.
- Future diagnostic export must redact accessible text by default and require explicit user action.
